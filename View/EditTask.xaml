<Window x:Class="TODOList.View.EditTask"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TODOList.View"
        xmlns:conv="clr-namespace:TODOList.Converters"
        mc:Ignorable="d"
        Title="Edit task" 
        Height="580" Width="400"
        ResizeMode="NoResize">

    <Window.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <Grid>
        <!-- Main grid -->
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="27*"/>
                <ColumnDefinition Width="170*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel x:Name="stackPanel" Grid.ColumnSpan="2" Margin="0,0,0,-106" Grid.RowSpan="3">

                <!-- Info input -->
                <Label HorizontalAlignment="Center" FontWeight="Bold" FontSize="20">Task</Label>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Label HorizontalAlignment="Center" VerticalAlignment="Center" Content=" Title"/>
                    <TextBox x:Name="textBoxTitle" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0" Grid.ColumnSpan="2"
                             Text="{Binding Path=Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="textBox_TextChanged"/>

                    <Label Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Content=" Description"/>
                    <TextBox x:Name="textBoxDescription" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,10,0" Grid.ColumnSpan="2"
                             Text="{Binding Path=Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="textBox_TextChanged"/>

                    <Label Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Content=" Due date"/>
                    <DatePicker x:Name="datePickerDueDate" 
                                Grid.Row="2" Grid.Column="1"
                                VerticalAlignment="Center" Margin="0,0,10,0" Grid.ColumnSpan="2"
                                SelectedDateChanged="datePickerDueDate_SelectedDateChanged"
                                SelectedDate="{Binding Path=DueDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="6" HorizontalAlignment="Center" VerticalAlignment="Center">Is Completed</Label>
                    <StackPanel Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <RadioButton x:Name="radioTrue" Content="True" GroupName="StatusGroup" Margin="0,0,10,0"
                                     IsChecked="{Binding IsCompleted, Mode=TwoWay}" />
                        <RadioButton x:Name="radioFalse" Content="False" GroupName="StatusGroup"
                                     IsChecked="{Binding IsCompleted, Converter={StaticResource InverseBooleanConverter}, Mode=TwoWay}" />
                    </StackPanel>

                </Grid>

                <!-- Buttons -->
                <Grid Height="50" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="buttonAdd" HorizontalContentAlignment="Center" Click="UpdateTask" Content="Update" Margin="0,10,0,10" HorizontalAlignment="Center" Width="177"/>
                    <Button Grid.Column="1" x:Name="butonCancel" HorizontalContentAlignment="Center" Click="Cancel_Click" Content="Cancel" Margin="10,10,10,10"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
