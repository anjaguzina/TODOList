<Window x:Class="TODOList.View.AddUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TODOList.View"
        mc:Ignorable="d"
        Title="Add New User" Height="500" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F6F8FC">

    <Window.Resources>
        <!-- Stil za zaobljena dugmad -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#CCE5FF"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stil za zaobljena i pastelna polja za unos -->
        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#E8F0FE"/>
            <Setter Property="BorderBrush" Value="#A3C9FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#E8F0FE"/>
            <Setter Property="BorderBrush" Value="#A3C9FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20" Width="420">
            <TextBlock Text="My Personal Info" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#333"/>

            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Username" Margin="5,0,0,2" FontWeight="SemiBold"/>
                <TextBox x:Name="UsernameTextBox" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Password" Margin="5,0,0,2" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordTextBox" Style="{StaticResource RoundedPasswordBox}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Name" Margin="5,0,0,2" FontWeight="SemiBold"/>
                <TextBox x:Name="NameTextBox" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,0,10">
                <TextBlock Text="Surname" Margin="5,0,0,2" FontWeight="SemiBold"/>
                <TextBox x:Name="SurnameTextBox" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,0,20">
                <TextBlock Text="Email" Margin="5,0,0,2" FontWeight="SemiBold"/>
                <TextBox x:Name="EmailTextBox" Style="{StaticResource RoundedTextBox}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Cancel" Style="{StaticResource RoundedButton}" Background="#FFB7B2" Foreground="White" Click="Cancel_Click"/>
                <Button Content="Confirm" Style="{StaticResource RoundedButton}" Background="#B5EAD7" Foreground="Black" Margin="20,0,0,0" Click="Confirm_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
